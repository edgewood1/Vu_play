// controls a part of the app - this is an instance through which the whole app, or a part of the app - they are like widgets / components
// take object as parameter, and pass through some methods / objects.. 
new Vue({
    el: "#vue-app", //which element on page will this control? 
    data: {
        name: "mel"
    }
}); 
